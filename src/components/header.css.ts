import { css } from '../stitches.config';

export const header = css({
  padding: 16,
  display: 'grid',
  gridAutoFlow: 'row',
  placeItems: 'center',
  gap: 16,
});

export const nav = css({
  display: 'flex',
  gap: 16,
});
